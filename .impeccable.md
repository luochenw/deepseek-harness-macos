## Design Context

### Users

面向把 DeepSeek Harness 当作日常开发环境使用的开发者。他们会长时间停留在同一会话中，一边与 Agent 协作，一边检查本地应用、阅读工作区文档、核对工具执行与代码产物；右侧区域不是最终结果附件栏，而是开发过程中的常驻辅助工作台。

### Brand Personality

清澈、专注、灵动。界面应保留现有“果冻海”的通透层次与原生 macOS 质感，同时让高频开发工具保持安静、紧凑、可信，不用装饰性视觉争夺内容注意力。

### Aesthetic Direction

- 延续现有亮色浅海水 / 深色深海玻璃的双外观，结构层使用中性半透明薄层。
- 青绿色只表示选中、运行、进度和可操作状态，不把整个工作台染成单一主色。
- 工作台采用原生开发工具的密度：窄工具栏、清晰分隔、可扫描标签，不使用营销式大标题或卡片堆叠。
- 浏览器页面与文档正文是视觉主体；外层 chrome 克制，所有圆角严格复用 `DSHRadius.sm/md/lg/xl`，不得在工作台另建一套数值。

### Design Principles

1. 对话始终是主任务，工作台通过可收起、可调宽的右侧 dock 提供并行上下文。
2. 标签代表稳定的工作对象；切换标签不能丢失浏览器页面状态、文档滚动位置或执行详情选择。
3. 用户可主动打开任意网页和 Markdown 文件；模型也可请求打开 HTTP(S) 页面或当前会话工作区内的 Markdown，但工作台不从属于 Agent 产出。
4. 浏览器和文档内容不进入 Agent 上下文，模型不能读取 DOM、注入脚本或自动点击；未来增加控制能力时必须按站点显式授权。
5. 复用现有 DSHTheme、中文界面与 SwiftUI/AppKit 原生模式，不引入独立视觉语言或 Web UI 外壳。
